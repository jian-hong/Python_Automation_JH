# RS1G00 NAND-2 -- JH DEMO DAY 2026-09-21. Grounded function CONFIRMED.
# On-disk DRAFT card is the SoT (ate_ds_extract/models missing). Unsigned vcc_grid / VOH / VOL HOLD. No invent.

part: RS1G00
model: RS1G00
component: Logic
package: SOT23
version: Version_1
sample_size: 4
year: "2026"
part_key: rs1g00
product_class: logic
vcc: 5.0
current_limit: 0.05
timing:
  timeout_s: 4.0
  settle_s: 0.3
enabled_tests:
  - tp
  - supply_current
  - input_threshold
  - icc
  - ii
fixture_modes:
  LOGIC:
    label: "Logic board (RS1G00 NAND)"
    board_class: general
    checklist:
      - "RS1G00 NAND-2. CONFIRMED function 2026-09-21. Unsigned VIH/VIL/VOH/VOL HOLD."
      - "oe none. IOZ OFF. ICCT ABSENT -- delta_icc from delta_icc_uA only; do not invent offset_v."
      - "Do not copy G08 CMOS bands. Do not tick voh/vol until CONFIRMED loads."
    tests: [tp, supply_current, input_threshold, icc, ii]

product_model:
  part: RS1G00
  status: CONFIRMED
  source_card: RS1G00_card_CONFIRMED.md
  confirm_note: "JH DEMO DAY 2026-09-21. Grounded OCR/card rows CONFIRMED (on-disk DRAFT card SoT; ate_ds_extract/models missing). Unsigned VIH/VIL/VOH/VOL HOLD. excel_plots gated to enabled series; status UNCONFIRMED (do not overstate unsigned loads)."
  package: SC70-5
  product_class: gate_nand2
  schmitt: false
  oe: none
  output_pin: Y
  output_type: push_pull
  logic_inputs:
  - A
  - B
  pins:
  - {name: A, role: input}
  - {name: B, role: input}
  - {name: GND, role: gnd}
  - {name: Y, role: output}
  - {name: VCC, role: vcc}
  pin_drive:
    A: {src: awg, ch: 1}
    B: {src: awg, ch: 2}
  truth_table:
    status: CONFIRMED
    kind: combinational
    note: NAND Y=NOT(A AND B). Function CONFIRMED from on-disk DRAFT card (JH DEMO DAY).
    rows:
    - {A: H, B: H, Y: L}
    - {A: L, B: H, Y: H}
    - {A: H, B: L, Y: H}
    - {A: L, B: L, Y: H}
  isolation:
    status: CONFIRMED
    note: NAND other=H -> invert. Do not copy G08 AND track.
    tests:
      A:
      - {sweep_pin: A, fix: {B: H}, y_expect: invert}
      B:
      - {sweep_pin: B, fix: {A: H}, y_expect: invert}
  vcc_grid:
    status: UNCONFIRMED
    stimulus: AWG
    note: No card VIH/VIL bands. Do not copy G08 CMOS 0.65/0.15. Numbers HOLD.
    ranges: []
    fixed_points: []
  pass_mode:
    icc: max_only
    voh: min_only
    vol: max_only
  dc_limits:
    VOH:
      status: UNCONFIRMED
      note: do not invent loads
    VOL:
      status: UNCONFIRMED
      note: do not invent loads
    ICCT_uA:
      status: ABSENT
      note: ICCT ABSENT -- do not invent offset_v / one_input_V
    delta_icc_uA:
      status: UNCONFIRMED
      note: Numbers HOLD. delta_icc from this block only; ICCT ABSENT is not a map
  recipe:
    settle_s: 0.05
    stable_n: 3
    stable_eps_V: 0.005
    stable_eps_A: null
    settle_timeout_s: 2.0
    runner: gate_nand2
    dual_channel_continue: false
  gaps:
  - CONFIRMED function 2026-09-21; unsigned vcc_grid / VOH / VOL / delta_icc HOLD; excel_plots UNCONFIRMED
  - NAND other=H invert; oe none; IOZ OFF
  - ICCT ABSENT -- do not invent ICCT map; delta_icc_uA only
  - do not invent VOH/VOL loads / VIH/VIL bands
  - stable_eps_A null
  data_paths:
    excel: "#Test_Database/{Component}/{Part}/{Package}/{Operator}/{Version_N}/workbook/"
    report: sessions/report.json
    datalog: sessions/datalog.md
    csv: sessions/csv/
    report_pdf: report.pdf
    records: "{test}/DUT_n/records/"
    attach: "{test}/DUT_n/"
  workbook_policy:
    golden_auto: one_per_version_overwrite
    auto: one_per_version_overwrite
    ultimate_manual: never_auto_write
    pretty: never_auto_write
  excel_plots:
    status: UNCONFIRMED
    series:
      - vih_vs_vcc
      - vil_vs_vcc
      - icc_vs_vcc
      - ii_vs_vcc
