---
keywords: logic-dc, rs1g97, rs1g126, setup_dc, schmitt, isolation, ioz, sim, pyvisa, run_params, goldens, see_lin, check_logic_campaign, test_database
main_idea: Logic DC failed on missing setup_dc plus visa-coupled family load. RS1G97/RS1G126 need YAML truth/isolation. Lab PC campaign FAIL is unsynced #Test_Database (RS29511/RS1G08/RS0204), not 97/126. goldens/see_lin is not in this git tree.
---

# Logic DC RS1G97 / RS1G126 (2026-09-17)

Hypothesis discarded: `seelim_dc.py` / `oe_timing.py` are not in this git tree. Part yaml was a stub (`tp` / `supply_current` / `input_thresholds`), not `icc`/`ii`. Owner is `lim`, not `seelim`. Local `seelim_dc.py` is a golden wrapper of `goldens/see_lin/<PART>/current_tests.py` with fallback `C:/Users/OoiJianHong/Downloads/See Lim Repo`. Cloud checkout has no `goldens/` files. Detect may scan those roots when present; do not ingest bodies into `ate/config`.

Root causes:

1. `generator_setup.setup_dc` did not exist. Every Ariff/RS0204 DC `run()` ImportError.
2. `configurations.py` annotated `ScopeDevice` before the class. `load_family("logic")` died via wraps `_load_legacy()` -> `logic_tests` -> `scope_setup`, and via `RunParams` imported from `runner` -> `mso5072`.
3. Ariff DC hardcodes AWG CH1/CH2 AND-gate holds. RS1G97 is 3-input Schmitt; RS1G126 has OE. Unused inputs must not float.
4. DEMO does not call `TestSpec.run`, so it can look green while START fails.

Lab PC (DESKTOP-12QOUB6, Python 3.14) fold-in:

1. `python -m ate.core.check_logic_campaign` FAIL missing `Logic/RS29511`, `Logic/RS1G08`, `Level/RS0204` campaigns. That is `TEST_DB_ROOT` / OneDrive `#Test_Database` sync, not RS1G97/RS1G126 DC. Check still fails if those folders are absent (not weakened).
2. `load_family("opamp")` / local `check_all_parts` (not in this repo) crash `ModuleNotFoundError: pyvisa` via `scope_setup` <- `configurations`. Install `requirements-console.txt` (already lists `pyvisa>=1.13.0`) for real USB. Logic family load must not require that.
3. `goldens/see_lin` MISS in cloud checkout. `golden_roots.yaml` lists `goldens/see_lin` and See Lim Repo as skipped-if-missing detect roots.

Fix: `ate/core/logic_model.py` + `ate/tests/logic/dc.py` driven by part YAML (`logic_inputs`, `oe`, truth, isolation, `vcc_sweep_list`). `RunParams` lives in `ate/core/run_params.py` so Logic tests do not import `runner`. `wraps.py` lazy-imports `logic_tests` only when START runs a wrap. SIM in `python -m ate.core.check_logic_dc`. Limits from in-repo extract only. No colleague-tree scrape. No `vih_vil` on RS1G97. No inherited RS1G08 VOH tables.
