# GT34 VOH LIVE session stamp (VOL NOT_RUN)

Date: 2026-09-18
Keywords: path-b, logic-dc, rs1gt34, VOH, VOL, LIVE_PASS, NOT_RUN, golden_auto, sessions, min_only, product_model.live, scale, 2Gxx, dual_channel_continue

## main_idea

JH room VOH LIVE SUCCESS on RS1GT34 (board still VOH-wired). Stamp five min_only PASS rows into `product_model.live` (sibling of `data_paths`, not a data_paths key). VOL stays NOT_RUN -- board change required; do not invent VOL measured. Scale path stays generic: CONFIRMED `dc_limits.VOH/VOL.loads` + truth_table all-high/all-low. Dual-channel Continue is 2Gxx only. Do not copy LIVE numbers onto other SKUs. Do not claim bench-green for other parts.

## traps

- `live` is a sibling of `data_paths`. Nesting it under `data_paths` breaks 97/126/34 exact-match DATA_PATH_TEMPLATES.
- Do not invent VOL live rows. Status NOT_RUN + note only.
- G07 VOH stays N_A/SKIP. Never invent VOH/IOZ on G07. Campaign vol_table stays 4 extract rows.
- LIVE is a session record, not Verify PASS and not a license to green other tests/SKUs.
- SHA chicken-egg: content SHA in operator doc; git HEAD is stamp.

## cite

ate/config/parts/rs1gt34.yaml
ate/tests/logic/product_model.py
ate/core/check_logic_dc.py
docs/LOGIC_DC_OPERATOR.md
docs/LOGIC_DC.md
